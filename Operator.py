a = 10
b = 3
print(a + b)    # 13
print(a - b)    # 7
print(a * b)    # 30
print(a / b)    # 3.333...
print(a % b)    # 1
print(a ** b)   # 1000
print(a // b)   # 3

# assignments operators
# =, +=, -=, *=, /=, %=, //=, **=, &=, |=, ^=, >>=, <<=

# = is used to assign a value to a variable.
#compound assignment operators are used to perform an operation and assign the result to the variable in one step.
# For example, a += b is equivalent to a = a + b.
# compound assignment operators are useful for writing concise code and can improve readability in some cases.

# logical operators
# and not or

